import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);

root.render(
  <React.StrictMode>
    <div>
      <h1>ProjectPulse AI</h1>
      <p>AI-driven project execution platform</p>
    </div>
  </React.StrictMode>
);
